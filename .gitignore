node_modules/
.expo/
dist/
web-build/
*.log
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.env
.env.local
# google-services.json / GoogleService-Info.plist are intentionally tracked
# (see README) — they're client identifying info baked into every built app,
# not a secret credential, and EAS Build needs them present in the repo to
# produce a working build.
firebase-adminsdk*.json
scripts/serviceAccountKey.json
.DS_Store
.vscode/
*.tsbuildinfo

admin/node_modules/
admin/dist/
admin/.env

functions/node_modules/
functions/.env
functions/lib/
